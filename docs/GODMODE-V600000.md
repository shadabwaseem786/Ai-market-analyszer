# V600000 — AI Ensemble Fusion + Model Orchestration

Adds ensemble voting, regime-dependent weighting, disagreement detection, stacking support,
feature/model drift detection, shadow models, explainability and safe fallback orchestration.

This is an orchestration framework. It does not claim a neural network is trained merely by
adding a module, and it does not execute trades.
