const clamp=(x,a,b)=>Math.max(a,Math.min(b,x));function norm(v){return clamp(Number(v)||0,-1,1)};exports.infer=f=>{const score=Number(f.score)||50,agreement=Number(f.agreementPct)||50,risk=Number(f.riskScore)||50,p=clamp(.5+.002*(score-50)+.0015*(agreement-50)-.0012*(risk-50),.02,.98),u=Math.round(clamp(100-Math.abs(p-.5)*200,5,95));return {aiProbability:Math.round(p*100),aiDirection:p>=.55?"BULLISH":p<=.45?"BEARISH":"NEUTRAL",model:"bounded ensemble decision layer",ensembleProbability:Math.round(p*100),uncertainty:u,confidence:Math.round((100-u)*.8)}};exports.handler=async event=>{try{const r=exports.infer(JSON.parse(event.body||"{}").features||{});return {statusCode:200,headers:{"content-type":"application/json","cache-control":"no-store","access-control-allow-origin":"*"},body:JSON.stringify({version:"V18100",...r,learningMode:"V18100-RESEARCH-ENSEMBLE",warning:"AI output is probabilistic decision support; it is not a validated profitability model."})}}catch(e){return {statusCode:400,body:JSON.stringify({version:"V18100",error:e.message})}}};
